# 蜃灵助手项目结构

本文件只用于快速定位代码。版本以 `manifest.json` 为准，施工计划、阶段记录与验收过程不放在这里。

## 根目录

```text
ShenLing-Extension/
├─ manifest.json       插件声明与版本
├─ index.js            插件入口、浮窗路由、设置页与模块装配
├─ style.css           全部界面样式与响应式规则
├─ README.md           用户说明
├─ 项目结构.md          本导航
├─ assets/             静态素材
└─ src/                功能源码
```

## src 总览

```text
src/
├─ constants.js        插件常量、存储 key、模块定义
├─ prompts.js          固定提示词、提示构造函数与统一登记表
├─ icons.js            Lucide 图标路径与渲染
├─ utils/
│  └─ text.js          文本、对象与格式化通用工具
├─ core/               酒馆接口、状态、API 与跨模块能力
└─ features/           各业务模块
```

## 核心层 `src/core/`

| 文件 | 功能 |
|---|---|
| `api.js` | 副 API 地址规范化、模型列表解析与请求路径处理 |
| `character.js` | 角色相关数据读取与规范化 |
| `chat.js` | 当前聊天、楼层消息和酒馆全局函数适配 |
| `context-resolver.js` | 角色卡、Persona、世界书、总结和模块上下文汇总 |
| `generation.js` | 统一 Generation Transport：主/副 API 调用、Feature 独立 timeout、副 API AbortController、GenerationTransportError、安全 diagnostics、主 API Provider 解析 |
| `logs.js` | 通讯日志创建、清洗、复制与展示数据 |
| `macros.js` | 提示词宏替换 |
| `message-fingerprint.js` | 中性消息正文指纹，供情感与好感 pending 对齐当前 swipe |
| `pending-commit.js` | `MESSAGE_SENT` pending 提交协调器与隔离执行 |
| `prompt-overrides.js` | 提示词本地覆盖规范化、协议校验、默认值解析、版本基线与 TXT 导出 |
| `prompt-state-lines.js` | 普通正文发送前仅剥离 emotion/affection 的 changed 控制行，保留状态数据；内部总结任务完整保留 |
| `settings.js` | 全局设置、聊天 metadata、各模块默认状态、提示词覆盖及旧总结模板迁移、好感设置/聊天态与采集状态模型 |
| `summary.js` | 总结 memory 文本解析、提示消息构造与相关辅助逻辑（通用 OpenAI 正文提取已收口到 `generation.js`） |
| `tavern-events.js` | SillyTavern 事件名称和监听适配 |
| `worldbook.js` | TavernHelper 世界书平台能力解析与统一 Provider |

## 功能层 `src/features/`

### `summary/` 自动总结

- `confirmed-lifecycle.js`：confirmed 回复捕获、每聊天持久化任务队列、刷新规范化与编辑/删除安全取消；本阶段不调用 Summary API。
- `confirmed-consumer.js`：confirmed 队列激活边界、当前聊天串行消费、请求前/写回前校验与失败恢复。
- `panel.js`：总结设置、状态和手动操作界面。
- `workflow.js`：通过 Generation Core 执行模型请求，并负责小总结、大总结、归档、重写和自动触发流程。

### `emotion-profile/` 情感档案

- `panel.js`：档案列表、编辑器和确认界面。
- `workflow.js`：情感变化提取、更新、注入与事件监听。

### `affection/` 好感度系统

- `model.js`：好感变化规范化、固定阶段边界、整数十分位账本重算与最新值手动调整。
- `panel.js`：好感度正式面板、当前 swipe pending 调整、角色详情、专属五阶段编辑副本与 overlay；第 1—8 步回归入口统一收在底部开发诊断折叠区。
- `workflow.js`：通过 Generation Core 生成专属五阶段表，并负责攻略提示词开关、affection/affection_first 解析、swipe pending、首次角色自动预建档、正式账本提交、手动校准、阶段切换/覆盖/重新生成，以及独立攻略状态正文注入与失效清理。

### `plot-outline/` 剧情大纲

- `panel.js`：大纲生成、编辑、章节和推进条件界面。
- `workflow.js`：通过 Generation Core 生成大纲，并负责解析、保存与上下文注入。

### `schedule/` 七日日程

- `model.js`：日程数据结构与标准化。
- `panel.js`：生成、查看、载入和清空界面。
- `workflow.js`：材料组装、生成、解析与保存。

### `memoir/` 回忆录与设定采集

```text
memoir/
├─ panel.js             剧情回忆、设定采集、草稿审阅与世界书选择器 UI
├─ workflow.js          回忆提炼、采集材料、通过 Generation Core 生成与严格解析草稿的流程
└─ worldbook-manager.js 回忆录世界书绑定、条目构造与读回核对
```

固定提示词及其登记信息统一位于 `src/prompts.js`；本地覆盖由 `src/core/prompt-overrides.js` 解析，memoir 目录不再单独存放 prompt 文件。

### `diary/` 日记

- `panel.js`：通过 Generation Core 生成日记，并负责草稿管理、日记本阅读、预设与导出。
- 素材位于 `assets/diary/`。

### `mini-theater/` 小剧场

小剧场在大型 Panel 内维护提示词、上下文、解析与预览业务，并通过 `src/core/generation.js` 发送主 / 副 API 请求。

- `panel.js`：提示词/文风收藏、生成、预览、编辑、保存与投放聊天。

### `prompt-editor/` 统一提示词编辑器

- `panel.js`：设置页提示词导航、会话草稿、协议状态、默认值对照、移动端 overlay、自测区与修改稿导出。

### `word-replace/` 词汇替换

- `core.js`：规则规范化、匹配和替换算法。
- `generated.js`：对模型生成结果应用替换。
- `panel.js`：规则管理、导入、查重与预览界面。

### `chat-beautify/` 聊天美化

- `field-config.js`：小总结与大总结字段展示配置，包含 affection 与 affection_first 协议。
- `render-memory.js`：`<memory>` 卡片渲染及好感变化/当前值显示格式。
- `render-grand-memory.js`：`<grand_memory>` 卡片渲染。
- `renderer.js`：聊天消息扫描、挂载与刷新入口。

### `context-diagnostics/` 上下文诊断

- `panel.js`：世界书上下文、缓存和 dry run 诊断界面。

## 静态素材 `assets/`

```text
assets/
└─ diary/               日记封面、纸张背景与移动端素材
```

## 常见任务入口

| 任务 | 首先查看 |
|---|---|
| 插件入口消失、模块无法打开 | `index.js`、`manifest.json` |
| 面板布局、主题、手机适配 | `style.css` 与对应模块 `panel.js` |
| 修改固定提示词 | `src/prompts.js` |
| 修改全局/聊天状态 | `src/core/settings.js` |
| 修改上下文材料读取 | `src/core/context-resolver.js` |
| 修改回忆录或设定采集 | `src/features/memoir/panel.js`、`workflow.js` |
| 修改世界书绑定或写入核对 | `src/features/memoir/worldbook-manager.js` |
| 修改模型调用链路 | `src/core/generation.js`、对应模块 `workflow.js`、`src/core/api.js` |
